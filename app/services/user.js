'use strict';
var app = angular.module('jobbomat');

app.service('User', ['$http', function($http) {

    var service = {
        set: function(field, value) {
        },
        reset: function() {
        },
        send: function() {
        }

    };

    return service;
}]);
